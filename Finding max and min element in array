#include <stdio.h>

int main() {
    int arr[7] = {32, 45, 12, 78, 56, 89, 23};
    int max = arr[0];
    int min = arr[0];

    for (int i = 1; i < 7; i++) {
        if (arr[i] > max) {
            max = arr[i];
        }
        if (arr[i] < min) {
            min = arr[i];
        }
    }

    printf("Maximum element: %d\n", max);
    printf("Minimum element: %d\n", min);

    return 0;
}

